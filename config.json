{
  "_comment": "Edit this file to customise venues, time windows, and notification settings.",

  "venues": [
    {
      "name": "Highbury Fields Tennis Courts",
      "slug": "highbury-fields-tennis-courts",
      "location": "islington",
      "activity": "tennis"
    },
    {
      "name": "Islington Tennis Centre",
      "slug": "islington-tennis-centre",
      "location": "islington",
      "activity": "tennis"
    }
  ],

  "time_windows": [
    { "start": "07:00", "end": "10:00", "label": "Morning" },
    { "start": "12:00", "end": "14:00", "label": "Lunchtime" },
    { "start": "17:30", "end": "21:00", "label": "Evening" }
  ],

  "days_ahead": 7,

  "_telegram_note": [
    "Set credentials via GitHub Secrets rather than storing them here.",
    "Required secrets: TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID",
    "",
    "Setup steps:",
    "  1. Open Telegram and message @BotFather",
    "  2. Send /newbot, choose a name and username → BotFather gives you a token",
    "  3. Start a chat with your new bot (so it can message you)",
    "  4. Visit https://api.telegram.org/bot<YOUR_TOKEN>/getUpdates in a browser",
    "     after sending the bot any message — you'll see your chat_id in the JSON",
    "  5. Add TELEGRAM_BOT_TOKEN and TELEGRAM_CHAT_ID as GitHub repo secrets"
  ],

  "_api_note": [
    "Better's booking API endpoint is discovered from browser network traffic.",
    "If the default endpoint stops working, set slot_api_url to the correct URL",
    "or override it with the BETTER_SLOT_API_URL environment variable.",
    "To find it: open DevTools → Network → XHR, then navigate to a venue booking",
    "page on better.org.uk and select a date."
  ],
  "slot_api_url": ""
}
